# Install Sass

```
Install Node.js

node -v
npm -v

npm install -g sass
sass --version

sass --watch input.scss output.css для слідкування за файлом
sass --watch app/sass:public/stylesheets для слідкування за папкою

npm install bootstrap@5.3.3

```